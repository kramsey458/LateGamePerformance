# Late Game Performance settings. Restart the game after editing.
#
# Nothing in this file changes what the game simulates, so players in a multiplayer game may have different
# values. What does decide that (the hauling cache, route maps, the tree and plant search) is not a setting: it is
# the same for everyone who runs the same version of the mod. Older files may still contain HaulCache,
# HaulCacheFlushEveryTicks, RouteMaps or YielderSearch; they are ignored.
#
# The verify keys only measure: they run the game's own code alongside, and log and count any difference, but the
# game is handed the mod's own result either way, so one player may switch them on alone. (Up to 0.4.26 most of
# them handed over the game's result on a difference, which in multiplayer made that player's game differ.)

# Recompute the vanilla list on every request and compare. Slower than vanilla; for testing only.
# Mismatches are logged and counted; the game still gets the cached list.
HaulCacheVerify = false

# Route maps are rebuilt in the background: the tick carries on and the main thread only ever waits for the one route map it is
# about to use. false = the main thread waits for the whole batch. Does not affect results, so
# it may differ between multiplayer peers.
RouteMapsBackground = true

# Fewer unbuilt route maps than this (a new building or two) are built directly on the main thread instead of on
# workers. They are still built straight away. Does not affect results, so it may differ between peers.
RouteMapsMinFields = 4

# Worker threads for route map rebuilds. 0 = automatic (up to 7). Does not affect results, so it may differ
# between multiplayer peers.
RouteMapsWorkers = 0

# Plant water check: read every water level again on the main thread with the game's own method and compare with
# what was computed on the water worker, and compare the levels known from the last pass with the stored ones. For
# testing. Mismatches are logged and counted; the worker's values and the known levels are still used.
PlantWaterVerify = false

# District resource counts: let the game count as well and compare. For testing. Mismatches are logged and counted,
# and the mod's numbers are put back.
DistrictCountsVerify = false

# Water map copy: let the game copy the water map every tick as well and compare, byte for byte, with the copy
# made on a worker thread. For testing. Mismatches are logged and counted; the worker's copy is still swapped in.
WaterMapCopyVerify = false

# Soil moisture and contamination: walk every soil cell the game's way as well and compare which cells were
# updated, whether they came from a worker's list or the scan. For testing. Mismatches are logged.
SoilScansVerify = false

# Home search: run the game's own walk for a beaver to move into a home as well and compare the beaver picked, and
# ask again every beaver whose "looking for a better home" answer was kept. For testing. Mismatches are logged and
# counted; the mod's pick still moves in.
HomeSearchVerify = false

# Every verify mode above and below at once, for the one correctness session a tester runs: the game's own code alongside
# each of this mod's replacements, every difference counted in the stats lines. Slower. Also a box on the in-game
# settings page. Measurement only, so it may differ between multiplayer peers.
VerifyAll = false

# Run the game's own terrain path search alongside the resumed one on a shadow field and count every difference.
# Measurement only: it never changes what the game is told. Slower; for testing. Also a box on the in-game settings
# page (Mods > Late Game Performance), which starts from this value and then remembers what was last chosen there.
TerrainSearchVerify = false

# Water rendering: switch water tiles only when they change and leave out texture uploads the graphics card
# already has. Rendering only, never the simulation, so it may differ between multiplayer peers. Set to false if
# water ever looks wrong (and please report it).
WaterRendering = true

# Autosaves and saves from the menu: the game thread takes the snapshot, and turning it into JSON, compressing it
# and writing the file happen on a worker thread while the game carries on. The file is written under a
# temporary name and renamed when it is complete. false = the game saves entirely by itself, as without the mod.
# Does not touch the simulation, so it may differ between multiplayer peers.
BackgroundSave = true

# Tree and plant search: run the game's own search as well and compare. Slower than no mod; for testing. Mismatches are logged and
# counted; the game still gets the mod's result.
YielderSearchVerify = false

# One stats line showing how the main thread's time splits between simulation and everything else (rendering,
# animation, UI), plus ticks and frames per second and the longest frame. One hook per frame; negligible cost.
Timing = true

# One line per save (autosave, manual save, save on exit) showing how long each stage of it took. Measurement
# only: the hooks run only during a save and do not change how or when the game saves. Also lets the Timing line
# report saves on their own instead of as its longest frame.
SaveTiming = true

# A profiling tool. true = switch on the game's own per-component tick timers and write their report to
# Documents\Timberborn\LateGamePerformance every MetricsEveryTicks ticks. The timers slow the game a little, so
# set it back to false when done. (Up to 0.4.9 this was a box on the settings page. Launching the game with
# -metrics has the same effect.)
RecordTimings = false

# How often the report above is written. 0 = never, which also disables RecordTimings.
MetricsEveryTicks = 3000

# Log whether incremental garbage collection is on, at startup.
GcReport = true

# Take the save snapshot of trees, crops, paths, levees and platforms on worker threads (only components this mod
# has read; everything else stays on the main thread). Saving only: what is saved does not change. false = the game
# takes the whole snapshot on the main thread.
SaveSnapshot = true

# At every save, take the game's own snapshot as well and compare every entity; the mod's is saved. Measurement only;
# slower saves. Also a box on the in-game settings page, which starts from this value and remembers the last choice.
SaveSnapshotVerify = false

# Place the audio listener only when the camera moved, while it is still gliding, and every tenth frame otherwise,
# instead of casting rays every frame. Sound only.
SoundListener = true

# Rebuild the status alert lists every sixteenth frame (the alerts with a value, such as a timed activation, every
# fourth), refresh the selected entity's panel every second frame and the top bar every fourth. User interface only.
UiThrottle = true

# Leave out the pose update of animated objects no part of which is on screen (shadows count as on screen); their
# animation time keeps running, so they are in the right pose when they come back into view. Rendering only.
AnimatorCulling = true

# Animated objects farther from the camera than AnimatorLodDistance tiles have their pose written every second frame,
# beyond twice that distance every fourth, beyond three times it every eighth; their animation time keeps running.
# Rendering only. AnimatorLod = false or a distance of 0 switches it off.
AnimatorLod = true
AnimatorLodDistance = 80

# The game hands every moved object to the physics engine every frame, although it asks physics only when a tool
# picks an object under the mouse. true = do that right before each such pick (and every 64th frame) instead, with
# the physics simulation mode set to Script (nothing in the game simulates physics). What a click selects does not
# change. User interface only.
DeferPhysicsSync = true

# Shaft and gear animations: their speed is worked out once per tick instead of once per animated part. The same
# speeds; presentation only.
ShaftAnimators = true

# After a long frame (an autosave, a garbage collection) the game runs the simulation for all the time that frame
# took, so one hitch becomes a second of stutter. true limits what one frame catches up to twice the ordinary frame
# time; the rest is left out. Pacing only: which ticks run, and in what order, is unchanged.
LimitCatchUp = true

# Time route map (flow field) rebuilds, need selection, walker path finding and the game's A* searches. Adds
# overhead to hot code; for investigation only. Also a box on the in-game settings page (Mods > Late Game
# Performance), which starts from this value and then remembers what was last chosen there.
Diagnostics = false

# While Diagnostics is on, the engine's own profiler counters sampled every frame and reported in the Diagnostics line:
# names separated by ';'. The file unity-markers.txt in Documents\Timberborn\LateGamePerformance, written once per session
# with Diagnostics on, lists every counter this build of the engine offers.
UnityMarkers = Camera.Render;Culling;Shadows.Draw;RenderForward;Render.OpaqueGeometry;Gfx.WaitForPresentOnGfxThread;Gfx.WaitForRenderThread;UIR.ImmediateRenderer;Animators.Update;ParticleSystem.Update;Draw Calls Count;SetPass Calls Count;Shadow Casters Count;Standard Draw Calls Count;SRP Batcher Draw Calls Count;Standard Instanced Draw Calls Count;Visible Skinned Meshes Count;GC.Collect

# Write a stats line to Player.log every N ticks (0 = never). 1000 ticks is 10 minutes at 1x speed.
StatsEveryTicks = 1000
